#cloud-config
package_update: true

packages:
  - nginx

runcmd:
  - systemctl enable nginx
  - systemctl start nginx
  - echo "<h1>This is a Auto-healing web tier - $(hostname)</h1>" > /var/www/html/index.html
